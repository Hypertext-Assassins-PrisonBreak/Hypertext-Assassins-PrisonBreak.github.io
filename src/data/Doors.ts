import Door from '../types/Door.js';

export default class Doors {
  public doors: Map<string, Door> = new Map<string, Door>([
    // ['door0', new Door(3, 6, false)],
    // ['door1', new Door(4, 8, true)],
    // ['door2', new Door(9, 8, true)],
    // ['door3', new Door(11, 4, false)],
    // ['door4', new Door(9, 3, true)],
    // ['door5', new Door(13, 3, true)],
    // ['door6', new Door(15, 6, false)],
    // ['door7', new Door(18, 2, true)],
    // ['door8', new Door(20, 4, false)],
  ]);
}
